[project]
name = "iamllm"
version = "0.4.0"
description = "A human-powered API compatible with OpenAI, Anthropic, and Gemini clients"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116,<1",
  "jinja2>=3.1,<4",
  "pillow>=11,<13",
  "python-dotenv>=1.1,<2",
  "python-multipart>=0.0.20,<1",
  "uvicorn[standard]>=0.35,<1",
]

[project.optional-dependencies]
dev = [
  "anthropic>=0.125,<1",
  "google-genai>=1.75,<2",
  "httpx>=0.28,<1",
  "openai>=2.54,<3",
  "pytest>=8.4,<9",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.packages.find]
include = ["app*"]
